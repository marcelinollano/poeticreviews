#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'
Bundler.require
Dotenv.load

Dir["./lib/*.rb"].each { |file| require file }

text = 'Monumental but .... Cleaning of the Puerta del Sol and Madrid. Do we need the town hall to commit?

Monumental pero.......Limpieza de la puerta del Sol y de Madrid. ¿Hace falta que el ayuntamiento se comprometa ?.'

text = GoogleMaps.cleanup(text)
text = Extract.split_more(text)
p text
