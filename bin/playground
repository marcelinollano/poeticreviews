#!/usr/bin/env ruby

require 'rubygems'
require 'bundler'
Bundler.require
Dotenv.load

Dir["./lib/*.rb"].each { |file| require file }

sentence = "Esto es una mierda"

p Extract.syntax(sentence)
puts
puts
p Extract.syllables(sentence)
puts
puts
p Extract.sentiment(sentence)
